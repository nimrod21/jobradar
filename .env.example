# Supavisor session-pooler connection string (IPv4).
# Supabase dashboard -> Connect -> Session pooler.
# The direct connection is IPv6-only — it will not work from hosts without IPv6.
DATABASE_URL=

USER_AGENT=jobradar/1.0 (+https://github.com/nimrod21/jobradar)
HTTP_CONCURRENCY=8
TIER3_ENABLED=false
